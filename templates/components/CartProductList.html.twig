<ul class="flex flex-col gap-6">
	{% for cartItem in cartItems %}
		<twig:CartProductListItem
			key="{{ cartItem.product.id }}"
			name="{{ cartItem.product.name }}"
			price="{{ cartItem.product.price }}"
			quantity="{{ cartItem.quantity }}"
		/>
	{% endfor %}
</ul>
