<ul class="wrapper flex flex-col gap-[120px] md:gap-[160px]">
    {% for product in products %}
        <twig:BaseListCategoryProductItem
            slug="{{ product.slug }}"
            name="{{ product.name }}"
            category="{{ product.category.name }}"
            isNew="{{ product.isNew }}"
            sourceImageLarge="{{ product.categoryImage.large }}"
            sourceImageMedium="{{ product.categoryImage.medium }}"
            sourceImageSmall="{{ product.categoryImage.small }}"
            description="{{ product.description }}"
        />
    {% endfor %}
</ul>
