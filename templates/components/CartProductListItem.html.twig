{% props
    key = 0,
    name = "",
    price = 0,
    quantity = 0,
%}

<li>
    <article class="flex justify-between items-center" >
        <div class="flex items-center gap-4">
            <figure>
                <twig:BasePicture
                    small="./images/cart/image-xx99-mark-two-headphones.jpg"
                    medium="./images/cart/image-xx99-mark-two-headphones.jpg"
                    large="./images/cart/image-xx99-mark-two-headphones.jpg"
                    class="h-16 w-16 rounded-lg"
                />
            </figure>
            <div>
                <p class="body font-bold uppercase">
                    {{ name }}
                </p>
                <p class="text-[14px] font-bold text-neutral-darkest/50">
                    $ {{ price }}
                </p>
            </div>
        </div>
        {% if app.request.attributes.get('_route') == "app_checkout" %}
            <twig:CartProductListItemCounter quantity="{{ quantity }}" key="{{ key }}" isReadOnly/>
        {% else %}
            <twig:CartProductListItemCounter quantity="{{ quantity }}" key="{{ key }}"/>
        {% endif %}
    </article>
</li>
